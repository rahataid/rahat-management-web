import { ProjectCreateFormView } from 'src/sections/project/add';

export const metadata = {
  title: 'Dashboard: Create a new project',
};

export default function ProjectCreatePage() {
  return <ProjectCreateFormView />;
}
