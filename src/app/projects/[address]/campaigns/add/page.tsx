import { ProjectCampaignCreateFormView } from '@sections/project/campaign/add';

export const metadata = {
  title: 'Dashboard: Create a new Campaign for Project',
};

export default function ProjectCreatePage() {
  return <ProjectCampaignCreateFormView />;
}
