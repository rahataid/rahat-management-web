import { CampaignCreateFormView } from '@sections/campaigns/add';

export const metadata = {
  title: 'Dashboard: Create a new campaign',
};

export default function CampaignCreatePage() {
  return <CampaignCreateFormView />;
}
