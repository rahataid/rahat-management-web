import { UserCreateFormView } from '@sections/administration/users/add';

export const metadata = {
  title: 'Dashboard: Create a new User',
};

export default function UserCreatePage() {
  return <UserCreateFormView />;
}
